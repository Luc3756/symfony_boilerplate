<div class="container-fluid">
    <div class="row flex-nowrap">
        <div class="col-auto col-md-3 col-xl-2 px-sm-2 px-0 bg-dark">
            <div class="d-flex flex-column align-items-center align-items-sm-start px-3 pt-2 text-white min-vh-100">
                <a href="{{ path('home') }}" class="d-flex align-items-center pb-3 mb-md-0 me-md-auto text-white text-decoration-none">
                    <span class="fs-5 d-none d-sm-inline">Menu</span>
                </a>
                 <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_login') }}">Login</a>
                </li>
                    {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_user_index') }}">UserList</a>
                </li>
                    {% endif %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_product_index') }}">ProductList</a>
                </li>
                    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}
                 <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_client_index') }}">ClientList</a>
                </li>
                    {% endif %}
                </ul>               
            </div>
        </div>
    </div>
</div>